@page
@model PersonalDataModel
@{
    ViewData["Title"] = "Informação Pessoal";
    ViewData["ActivePage"] = ManageNavPages.PersonalData;
}

<h4>@ViewData["Title"]</h4>

<div class="row">
    <div class="col-md-6">
        <p>Sua conta possui informação pessoal que nos forneceu. Essa página o permite baixar ou deletar essa informação</p>
        <p>
            <strong>Deletando essa informação é um processo permanente e não poderá ser revertido.</strong>
        </p>
        <form id="download-data" asp-page="DownloadPersonalData" method="post" class="form-group">
            <button class="btn btn-primary" type="submit">Baixar</button>
        </form>
        <p>
            <a id="delete" asp-page="DeletePersonalData" class="btn btn-secondary">Deletar</a>
        </p>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
